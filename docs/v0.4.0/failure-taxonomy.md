# Failure Taxonomy

The benchmark layer uses explicit codes for missing evidence, schema gaps, lexical mismatch, semantic false positives, filter and constraint failures, employer/client ambiguity, delivery-mention ambiguity, fusion displacement, reranker regression, candidate-pool misses, stale experience, near duplicates, judgement gaps, planner routing errors, backend inconsistency, and graph-relationship needs.
